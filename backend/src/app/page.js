export default function ApiHome() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 32 }}>
      <h1>easyShop API</h1>
      <p>Backend is running. Use /api/health to check service health.</p>
    </main>
  );
}
